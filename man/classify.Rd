% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/classify.R
\name{classify}
\alias{classify}
\title{Column B of the Reporting Card: use-case tier on EMR_panel}
\usage{
classify(
  emr_upper,
  emr_lower = NA_real_,
  use_case = c("research", "screening", "diagnostic", "clinical", "custom"),
  tolerance = NULL,
  tolerance_next = NULL,
  emr_point = NA_real_,
  regime = NULL
)
}
\arguments{
\item{emr_upper}{Numeric scalar. Upper bound of the 95\% CI on EMR_panel.}

\item{emr_lower}{Optional numeric scalar. Lower bound of the 95\% CI on
EMR_panel. \code{NA} or \code{NULL} collapses \code{Fails} into \code{Indeterminate}.}

\item{use_case}{One of \code{"research"}, \code{"screening"}, \code{"diagnostic"},
\code{"clinical"}, or \code{"custom"}. Sets the default tolerance \code{T} from
\code{\link[=grass_use_case_ladder]{grass_use_case_ladder()}}; \code{"custom"} requires \code{tolerance}.}

\item{tolerance}{Optional numeric scalar. Overrides the use-case default.
Required when \code{use_case = "custom"}.}

\item{tolerance_next}{Optional numeric scalar. Overrides \code{T_next}
(used by the \code{Exceeds} tier). Defaults to the next harder use case on
the ladder; \code{NA} at the top of the ladder disables \code{Exceeds}.}

\item{emr_point}{Optional numeric scalar. EMR_panel point estimate.
Stored on the output for the citation sentence; not used in tier
assignment.}

\item{regime}{Optional \code{grass_asymmetry} object from \code{\link[=check_asymmetry]{check_asymmetry()}}.
Attached so that a Reporting Card row carries both Column A and
Column B together.}
}
\value{
An S3 object of class \code{grass_classification} with fields:
\itemize{
\item \code{tier}: character tier label
\item \code{tolerance}: \code{T} used (with source)
\item \code{tolerance_next}: \code{T_next} used (or \code{NA})
\item \code{emr_upper}, \code{emr_lower}, \code{emr_point}: inputs, echoed for reporting
\item \code{margin}: \code{T - emr_upper} (positive means the panel sits below T)
\item \code{use_case}: the use-case label
\item \code{regime}: attached Column A (or \code{NULL})
}
}
\description{
\code{classify()} assigns the use-case tier — one of \code{"Fails"},
\code{"Indeterminate"}, \code{"Meets"}, \code{"Met with distinction"}, or \code{"Exceeds"} —
given the 95\% CI on EMR_panel and a declared tolerance \code{T}. This is
Column B of the §8 Reporting Card in the merged GRASS binary-rater-
reliability paper.
}
\details{
The label sits on EMR_panel (a rate, a function of \verb{q̂}, \verb{F̂}, \code{k}, and
the voting rule) rather than on any individual agreement coefficient.
That is what makes the label system regime-conditioned, metric-agnostic,
and operationally consequential — the four specific defects of
Landis-Koch (1977) and Koo-Li (2016). See \code{paper2/review/framework_notes.md}
§0.6.5 for the full argument.

Tier partition (on the 95\% CI \verb{[L, U]} of EMR_panel, with tolerance
\code{T}):\tabular{lll}{
   Tier \tab Arithmetic \tab Meaning \cr
   \code{Exceeds} \tab \code{U < T_next} \tab Would pass a more demanding use case \cr
   \verb{Met with distinction} \tab \code{U < T/2} (and not Exceeds) \tab Passes with a safety margin within the declared use case \cr
   \code{Meets} \tab \verb{T/2 <= U < T} \tab Passes the declared use case \cr
   \code{Indeterminate} \tab \verb{L < T <= U} \tab CI straddles T; data too thin to call \cr
   \code{Fails} \tab \code{L >= T} \tab CI entirely at or above T; panel fails the declared use case \cr
}


If \code{emr_lower} is \code{NA} the \code{Fails} tier collapses into \code{Indeterminate}
(without a lower bound the function cannot distinguish "CI straddles"
from "CI entirely above").
}
\examples{
# A screening panel, symmetric raters, tight CI
asym <- check_asymmetry(se = c(0.86, 0.88, 0.84, 0.87, 0.85),
                        sp = c(0.85, 0.87, 0.86, 0.86, 0.87))
classify(emr_upper = 0.07, emr_lower = 0.05, use_case = "screening",
         emr_point = 0.06, regime = asym)

# Same panel held to the diagnostic tolerance — indeterminate
classify(emr_upper = 0.07, emr_lower = 0.03, use_case = "diagnostic",
         emr_point = 0.05)

# Custom tolerance
classify(emr_upper = 0.04, use_case = "custom", tolerance = 0.08)
}
\seealso{
\code{\link[=check_asymmetry]{check_asymmetry()}} for Column A; \code{\link[=emr_panel]{emr_panel()}} to compute the
EMR_panel point estimate from \verb{q̂} and \code{k} under the Tier 1 diagonal.
}
