% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/classify.R
\name{emr_panel}
\alias{emr_panel}
\title{Panel-level misclassification rate under k-rater majority vote}
\usage{
emr_panel(q, k)
}
\arguments{
\item{q}{Numeric scalar or vector in \verb{[0, 1]}. Per-rater accuracy
(\code{Se = Sp = q}).}

\item{k}{Integer, odd, \verb{>= 3}. Number of raters voting.}
}
\value{
Numeric vector of EMR_panel values, same length as \code{q}.
}
\description{
Under the Se = Sp = q diagonal (GRASS Tier 1), the panel's majority
vote across \code{k} raters agrees with the latent class with probability
\code{P(Bin(k, q) >= ceiling(k/2))}. \code{emr_panel()} returns \verb{1 -} that
probability, which is the rate at which the panel's decision
misclassifies a randomly drawn subject.
}
\details{
Only odd \code{k} is supported in this release; even \code{k} requires a
tie-breaking rule that the framework has not yet declared.
}
\examples{
emr_panel(q = 0.85, k = 5)
emr_panel(q = c(0.80, 0.85, 0.90, 0.95), k = 5)
}
