% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/spec.R
\name{grass_spec_binary}
\alias{grass_spec_binary}
\title{Binary two-rater agreement spec}
\usage{
grass_spec_binary(reference_level = 0.85)
}
\arguments{
\item{reference_level}{One of \code{0.70}, \code{0.80}, \code{0.85}, \code{0.90}. Selects the
Se = Sp rater-quality band whose analytical Youden-J reference curve
is attached to the result. \code{0.85} is the default and reproduces the
"high" quality curve from prior releases; \code{0.70} reproduces the
"medium" curve. \code{0.80} and \code{0.90} are new in 0.1.2. Pass \code{NULL} to
skip the reference attachment entirely (metrics in context only).}
}
\value{
A \code{grass_spec_binary} object carrying \verb{$family = "binary"} and
\verb{$reference_level}.
}
\description{
The spec for the currently-implemented metric family: Cohen's kappa,
PABAK, and Gwet's AC1 for two raters on a binary outcome, evaluated
against a prevalence-conditioned reference curve at the chosen
\code{reference_level}.
}
\examples{
grass_spec_binary()
grass_spec_binary(reference_level = 0.80)
}
